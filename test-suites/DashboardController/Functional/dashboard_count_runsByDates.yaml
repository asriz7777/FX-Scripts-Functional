name: dashboard_count_runsByDates

authors:
  - Asiya Shaji

endpoint: /dashboard/count-runs-between?oct?tonov

method: GET

auth: Default

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"

severity: Major

category: Functional

tags:
    - V2
