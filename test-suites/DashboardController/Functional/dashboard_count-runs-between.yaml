name: dashboard_count-runs-between

authors:
  - Asiya Shaji

endpoint: /dashboard/count-runs-between

method: GET

auth: Default

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"

severity: Major

category: Functional

tags:
  -
