name: dashboard_count_bytes

authors:
  - Asiya Shaji

endpoint: /dashboard/count-bytes

method: GET

auth: Default

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"

severity: Major

category: Functional

tags:
    - V2
